# Backup/Restore

_Placeholder._ Maps to **System > Backup/Restore** (`view_system_backup.cfm`).

<!-- TODO: cover the Docker-aware backup/restore once #219 + #220 ship
     (system_backup.sh + system_restore.sh refactor: 4 scopes —
     system / archive / vmail / nextcloud / all). Until then,
     document the legacy approach with caveats. -->
