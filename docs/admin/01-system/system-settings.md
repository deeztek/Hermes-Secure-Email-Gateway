# System Settings

_Placeholder._ Maps to **System > System Settings** (`view_system_settings.cfm`).

<!-- TODO: cover the `system_settings` and `parameters2` tables and how
     they're surfaced through the admin UI. License/edition state lives
     here (see CLAUDE.md "Pro Edition License Validation"). -->
