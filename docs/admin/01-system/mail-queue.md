# Mail Queue

_Placeholder._ Maps to **System > Mail Queue** (`view_mail_queue.cfm`).

<!-- TODO: cover the Mail Queue admin page — `postqueue -p`, flush, hold,
     release, delete operations via `docker exec hermes_postfix_dkim`.
     Note this is the queue for hermes_postfix_dkim, not hermes_dovecot
     or hermes_mail_filter. -->
