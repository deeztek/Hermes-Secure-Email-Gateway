# Authentication Settings

_Placeholder._ Maps to **System > Authentication Settings** (`view_authentication_settings.cfm`).

<!-- TODO: cover Authelia + LDAP backing the admin/user/Webmail surfaces,
     the cn=admins / cn=mailboxes / cn=relays / cn=one_factor /
     cn=two_factor / cn=nextcloud groups, 2FA enforcement (#225 ships
     this UX), session refresh_interval, password policy. -->
