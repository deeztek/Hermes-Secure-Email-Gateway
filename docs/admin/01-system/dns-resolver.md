# DNS Resolver

_Placeholder._ Maps to **System > DNS Resolver** (`view_dns_resolver.cfm`).

<!-- TODO: cover Unbound local DNS, custom forwarders, the gotcha around
     local-data overrides (per 4/20 + 4/21 session memories), and how
     containers consume DNS (resolved via host or via
     hermes_unbound). -->
