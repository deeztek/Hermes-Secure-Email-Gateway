# Console Settings

_Placeholder._ Maps to **System > Console Settings** (`view_console_settings.cfm`).

<!-- TODO: cover console host (FQDN/IP) change, cascading regeneration of
     nginx vhost + Authelia + Nextcloud external-sites entry. Reference
     the per-template re-render gotcha (`feedback_stale_generated_vhost`). -->
