# LDAP RemoteAuth

_Placeholder._ Maps to **System > LDAP RemoteAuth** (`view_remoteauth.cfm`).

<!-- TODO: cover the upstream-AD/LDAP RemoteAuth path — domain mappings,
     `seeAlso` + `associatedDomain` overlay, DN patterns with
     {firstname}/{lastname}/{email} placeholders, deletion validation
     (#102 still pending), CSV bulk-add for relay recipients. -->
