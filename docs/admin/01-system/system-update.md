# System Update

_Placeholder._ Maps to **System > System Update** (`view_system_updates.cfm`).

<!-- TODO: cover the Docker-aware update workflow once #221 ships
     (system_update.sh refactor + JSON endpoint at validate.hermesseg.io
     per #218). Until then, document the manual `docker compose pull`
     route. -->
