# Admin Console Firewall

_Placeholder._ Maps to **System > Admin Console Firewall** (`view_console_firewall.cfm`).

<!-- TODO: cover the IP-allowlist mechanism that restricts access to
     /admin and /admin/2. Recovery path if admin gets locked out
     (Hermes CLI Management Console — #173 once it lands). -->
