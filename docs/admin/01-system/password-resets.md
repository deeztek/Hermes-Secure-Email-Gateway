# Password Resets

_Placeholder._ Maps to **System > Password Resets** (`view_password_reset_requests.cfm`).

<!-- TODO: cover the password reset request workflow — math CAPTCHA on
     forgot_password.cfm, token expiry, admin override path. Reference
     the configurable CAPTCHA provider TODO in CLAUDE.md once that
     ships. -->
