# SMTP TLS Settings

_Placeholder._ Maps to **System > SMTP TLS Settings** (`view_smtp_tls_settings.cfm`).

<!-- TODO: cover smtp_tls_security_level (Disabled/Opportunistic/Mandatory)
     + per-domain TLS Policy entries. Cross-link to per-domain
     enforce_tls toggle on Email Relay > Domains. -->
