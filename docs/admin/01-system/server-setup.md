# Server Setup

_Placeholder._ Maps to **System > Server Setup** (`view_server_setup.cfm`).

<!-- TODO: cover initial server-setup workflow (post-install configuration
     wizard) — admin password, console host, primary domain, mail-routing
     defaults. Pairs with #217 install-script refactor. -->
