# Mailbox Rules

_Placeholder._ Maps to **Email Server > Mailbox Rules** (`view_sieve_rules.cfm`, admin-side).

<!-- TODO: admin-side global sieve rules (scope='global'). Distinguish from /users/2/view_sieve_rules.cfm (per-user, scope='user'). -->
