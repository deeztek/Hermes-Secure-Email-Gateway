# Aliases

_Placeholder._ Maps to **Email Server > Aliases** (`view_mailbox_aliases.cfm`).

<!-- TODO: mailbox-side aliases — alias-to-mailbox mappings, silent-discard option (memory: project_mailbox_aliases). -->
