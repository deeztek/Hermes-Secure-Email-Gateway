# Domains

_Placeholder._ Maps to **Email Server > Domains** (`view_mailbox_domains.cfm`).

<!-- TODO: mailbox domain list + Add/Edit modals — default quota, catchall, Nextcloud enabled, enforce_mfa slider (#225). Distinguish from relay domains. -->
