# DMARC Settings

_Placeholder._ Maps to **Content Checks > DMARC Settings** (`view_dmarc_settings.cfm`).

<!-- TODO: OpenDMARC integration + DMARC reporting (Net::SMTP path in hermes_dmarc, per project_dmarc_net_smtp memory + #215 Perl @ fix). -->
