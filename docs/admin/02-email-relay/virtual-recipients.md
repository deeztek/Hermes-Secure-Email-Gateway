# Virtual Recipients

_Placeholder._ Maps to **Email Relay > Virtual Recipients** (`view_virtual_recipients.cfm`).

<!-- TODO: virtual_recipients table — alias mappings + catch-all (`@domain`). Catch-alls reflected in /users/ Quarantined/Total Messages/Message History queries via special branch (Phase 2 fix). Specific aliases not pulled — known parity gap. -->
