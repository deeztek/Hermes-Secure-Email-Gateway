# Domains

_Placeholder._ Maps to **Email Relay > Domains** (`view_domains.cfm` / `edit_domain.cfm`).

<!-- TODO: relay domain list + Add/Edit modals — delivery method, destination, MX, auth, enforce_tls. Distinguish from mailbox domains. -->
