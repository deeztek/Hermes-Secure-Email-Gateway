# Shared Folders

_Placeholder._ Maps to **Shared Folders** (`view_shared_folders.cfm`). Mailbox-only.

<!-- TODO: user-managed folder shares — user_folder_shares table, Dovecot 2.4 vfile ACL regeneration via sync_user_folder_acl_file.cfm. NC Mail caches folder tree, recipients must remove+re-add account to see new shares. -->
