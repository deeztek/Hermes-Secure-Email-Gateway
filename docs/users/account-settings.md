# Account Settings

_Placeholder._ Maps to **Account Settings** (`user_settings.cfm`).

<!-- TODO: password change (local-auth only — remote-auth users can't change here), recovery email, timezone, 4-state 2FA card with branched mailbox vs relay copy (#225 Phase 2). -->
