# OpenARC -> syslog routing (mail facility, info level).
# Matches the opendmarc/opendkim pattern; lets docker-logs tail show
# real-time ARC activity for troubleshooting.

if $programname == 'openarc' then -/var/log/openarc.log
& stop
