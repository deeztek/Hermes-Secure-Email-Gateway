hosts = hermes_db_server
dbname = hermes
user = HERMES-USERNAME
password = HERMES-PASSWORD
query = SELECT DISTINCT 'REJECT this address only accepts mail from inside the organization' FROM mailbox_aliases WHERE alias_address='%s' AND internal_only=1
